name: HACS Validation

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  hacs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1

      - name: HACS Action
        uses: hacs/action@22.5.0
